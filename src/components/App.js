import { hot } from 'react-hot-loader/root';
import React from 'react';

const App = () => {
  // const {
  //   components: { Welcome },
  // } = welcome;

  return (
    <div className="App">
      <h1>Welcome</h1>
    </div>
  );
};

export default hot(App);
